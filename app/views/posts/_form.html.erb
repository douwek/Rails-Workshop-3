<%= form_for(@post, :html => {:class => "well"}) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%#= f.label :bericht %><br />
    <%= f.text_area :bericht, :rows => 2, :style => "width: 640px;" %>
  </div>
  <div class="actions">
    <%= f.submit "Opslaan", :class => "btn btn-primary btn-large" %>
  </div>
<% end %>
